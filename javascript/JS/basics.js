//VAriables, data types and expressions/statements

// 1-) Variables => Container for storing data temporary 

// 2-) Datatype =>Defines the shape of data to be used during program execution

// primitif and Refernece type
// eg:strings , number , boolean,object,function,null,undefined,void

// 3-)Expression
let age = (30 + 5);
//4-) Operators 
// + - * / % += -= *= ** -- ++ && ! ||

// Reference type
           //OBJECTS
let Person = {
    name:'',
    age:23
}
 let human  = new Object();
 let testObj = Object.create()

        //ARRAYS
let score = [1,3,4];
let newArra = new Array();

    //Array Mehtods
//map, filter,forEach, find ,indexOf,sort, reverse, concat,some,includes,
//  reduce, push, pop,unShift,Shift,splice
    
           //high order array functions =>are functions that execute a certain function in each item in an array
           
//map , filter , forEach , find , reduce , sort , 
